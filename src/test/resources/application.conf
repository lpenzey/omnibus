database = {
  url = "jdbc:h2:mem:test1;MODE=MySQL;INIT=RUNSCRIPT FROM 'classpath:create_tables.sql'"
  user = "root"
  password = "dbpassword"
  driver = org.h2.Driver
  connectionPool = disabled
  keepAliveConnection = true
}