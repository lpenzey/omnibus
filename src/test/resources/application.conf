database = {
  url = "jdbc:h2:mem:test1;MODE=MySQL;INIT=RUNSCRIPT FROM 'classpath:create_users_table.sql'\\;RUNSCRIPT FROM 'classpath:testdata.sql'"
  user = "root"
  password = "dbpassword"
  driver = org.h2.Driver
  connectionPool = disabled
  keepAliveConnection = true
}